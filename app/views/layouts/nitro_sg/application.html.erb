<!DOCTYPE html>
<html>
<head>
  <title>Nitro sg</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_pack_tag 'main' %>
  <%= javascript_pack_tag 'application' %>
</head>
<body>
  <div class="container">
    <main>
      <div class="main"><%= yield %></div>
      <div class="side-nav">
        <% Dir["#{NitroSg::Engine.root}/app/pbKits/*"].each do |link| %>
          <%= File.basename(link) %>
        <% end %> 
      </div>
    </main>
  </div>
</body>
</html>
