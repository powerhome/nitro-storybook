import React from 'react'


type Props = {
  //<!-- List props and their type here -->
}

export default class <%= js_titleize_file_name %> extends React.Component<Props> {
  static defaultProps = {
    //<!-- List prop default values here -->
  }
  props: Props
  render() {

    const {
      //<!-- List props here -->
    } = this.props

    return (<!-- Build component here -->)
  }
}
