// This is javscript code, which uses ERB tags for templating
// All the methods will be invoked and their values will be interpolated

import React from 'react'


type Props = {
  //<!-- List props and their type here -->
}

export default class <%= js_file_name %> extends React.Component<Props> {
  static defaultProps = {
    //<!-- List prop default values here -->
  }
  props: Props
  render() {

    const {
      //<!-- List props here -->
    } = this.props

    return (<!-- Build component here -->)
  }
}
