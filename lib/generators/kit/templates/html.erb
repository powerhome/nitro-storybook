<%
  props ||= {}
  componentclass = props.fetch(:componentclass, "pb-#{name}")
  text = props.fetch(:text, "I am pb-#{name}")
%>

<span class="<%= componentclass %>">
    <span><%= text %></span>
</span>
